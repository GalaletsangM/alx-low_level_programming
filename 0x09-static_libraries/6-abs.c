#include "main.h"
/**
 * _abs - finds the absolute value of an integer
 * @a: integer
 * Return: prints integer without a sign
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = a * -1;
		return (a);
	}
	return (a);
	return (0);
}
