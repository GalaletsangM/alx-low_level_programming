#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _atoi - converts a string to an integer
 * @s: string to be converted
 * Return: integer
 */
int _atoi(char *s)
{
	return (atoi(*s));	
}
